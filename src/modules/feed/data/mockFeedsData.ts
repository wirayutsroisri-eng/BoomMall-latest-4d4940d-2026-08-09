import type { FeedItem } from '../domain/types';

/** Feed catalog is live-only — no simulated clips. */
export const mockFeedsData: FeedItem[] = [];

export { SEED_FOLLOWING_HANDLES } from './seedFollows';
