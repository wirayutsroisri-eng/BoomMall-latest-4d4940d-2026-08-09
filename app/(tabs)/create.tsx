import React from 'react';
import { View } from 'react-native';

/** Placeholder tab — actual create UI opens via /create-modal */
export default function CreateTabPlaceholder() {
  return <View />;
}
